# Copy this file to ".env" (same folder) and paste your own key below.
# Get a free key (no credit card) at: https://aistudio.google.com/apikey
#
# IMPORTANT: rename this copy to ".env" — the script only reads a file
# named exactly ".env", not ".env.example".
#
# Never commit your real .env file to git / share it with anyone —
# it's your personal key. This .env.example file (with no real key
# in it) is the one that's safe to share or commit.

GEMINI_API_KEY=AIza_paste_your_own_key_here
